import React, { memo } from 'react';

import { ScrollAreaProps } from './types';

export default function ScrollArea({ children }: ScrollAreaProps) {
  throw new Error('ScrollArea.native.tsx not implemented!');
}
