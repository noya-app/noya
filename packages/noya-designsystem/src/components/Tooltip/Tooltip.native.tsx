import React, { memo } from 'react';

import { TooltipProps } from './types';

const Tooltip: React.FC<TooltipProps> = (props) => {
  throw new Error('Tooltip.native.tsx not implemented!');
};

export default memo(Tooltip);
