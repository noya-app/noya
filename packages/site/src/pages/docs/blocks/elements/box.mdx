import { boxSymbolId } from '../../../../ayon/blocks/symbolIds';

## Live Preview

You can double click the block to edit its contents.

<InteractiveBlockPreview blockId={boxSymbolId} />

## Configuration

The box is an extremely configurable block. It can be used to create a wide
variety of layouts and styles using the many available `#` commands.

<InteractiveBlockPreview
  blockId={boxSymbolId}
  blockText="#rounded-2xl #bg-emerald-500"
/>
