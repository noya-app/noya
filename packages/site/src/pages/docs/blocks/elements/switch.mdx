import { switchSymbolId } from '../../../../ayon/symbols/symbolIds';

## Live Preview

You can double click the block to edit its contents.

<InteractiveBlockPreview symbolId={switchSymbolId} />

## Configuration

The switch has a few different states that can be configured by adding:

- `#off` - to switch to the off position (default)
- `#on` - to switch to the on position
- `#disabled` - to disable the switch

Here's an example with `#on` and `#disabled`:

<InteractiveBlockPreview symbolId={switchSymbolId} blockText="#on #disabled" />
