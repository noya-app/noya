import { FileDropTargetProps } from './types';

export default function FileDropTarget<T extends string>(
  props: FileDropTargetProps<T>,
) {
  throw new Error('FileDropTarget.native.tsx is not implemented!');
}
